const Appointment = require('../models/appointment');

// GET Appointment list for a given Practitioner id, between date range
exports.search = (req, res) => {
    res.send('NOT IMPLEMENTED: Appointment search');
};
